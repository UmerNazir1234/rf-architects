import SectionLoader from '@/components/SectionLoader'
import React from 'react'

const loading = () => {
    return (
        <SectionLoader class='h-screen fixed inset-0 w-full h-full' />
    )
}

export default loading